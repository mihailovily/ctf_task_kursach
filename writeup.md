# Writeup: «Странный QR»

## Условие

> Нам в руки попал как-то странный qr, стоит держать ушки на макушке

Дан файл `main_task/task.png`. Нужно найти флаг.

Флаг:

```text
zssoib{b0nc4_CTF_1s_th3_b3st}
```

## 1. Считываем QR-код

На первый взгляд перед нами обычный QR-код. После его декодирования получаем файл `task.png`.

![QR-код](main_task/task.png)

Само содержимое QR не является финальным ответом, поэтому продолжаем исследовать полученный PNG как файл.

## 2. Проверяем метаданные PNG

Для начала смотрим метаданные:

```bash
exiftool task.png
```

В выводе встречаются подозрительные подсказки:

```text
Image Description : this is the wrong way also
Warning            : Trailer data after PNG IEND chunk
```

Особенно важна последняя строка. PNG заканчивается chunk-ом `IEND`, но после него у файла ещё есть данные. Значит, нужно посмотреть хвост файла в hex-редакторе или извлечь байты после `IEND`.

![Метаданные PNG](документация/скриншоты/Снимок%20экрана_20240403_215422.png)

## 3. Извлекаем trailer после IEND

В hex-редакторе находим сигнатуру конца PNG:

```text
49 45 4E 44 AE 42 60 82
```

После неё находится строка:

```text
therewasatbash:yDU5BnElACQoLns0wSYaLr8exTUawTErzD4fB29gO3Qswb9YxVkFESJ0JJ==
```

Префикс `therewasatbash` подсказывает первый шаг декодирования — шифр Atbash.

![Trailer после IEND](документация/скриншоты/Снимок%20экрана_20240403_215858.png)

## 4. Применяем Atbash и Base64

Заменяем буквы по правилу Atbash: `a ↔ z`, `b ↔ y`, `A ↔ Z` и так далее. Получаем:

```text
bWF5YmVoZXJlOmh0dHBzOi8vcGFzdGViaW4uY29tL3Jhdy9BcEpUVHQ0QQ==
```

Теперь декодируем Base64:

```bash
echo 'bWF5YmVoZXJlOmh0dHBzOi8vcGFzdGViaW4uY29tL3Jhdy9BcEpUVHQ0QQ==' | base64 -d
```

Результат:

```text
maybehere:https://pastebin.com/raw/ApJTTt4A
```

![Результат Base64](документация/скриншоты/Снимок%20экрана_20240403_223051.png)

## 5. Изучаем Pastebin

Переходим по ссылке из предыдущего шага. На Pastebin находится закодированная программа и таблица замен:

```text
# + => %
# - => ?
# [ => )
# ] => (
# . => :
```

Таким образом, для обратного преобразования применяем:

| Закодированный символ | Brainfuck |
|---|---|
| `&` | `+` |
| `?` | `-` |
| `)` | `[` |
| `(` | `]` |
| `:` | `.` |
| `~` | `>` |
| `;` | `<` |


## 6. Декодируем Brainfuck

В репозитории уже сохранен скрипт [`useful_things/decoder.py`](useful_things/decoder.py). Его задача — заменить специальные символы обратно на команды Brainfuck.

```bash
cd useful_things
python3 decoder.py
```

На выходе появляется Brainfuck-программа. Её также можно получить вручную, последовательно применив таблицу замен.

![Скрипт декодирования](документация/скриншоты/Снимок%20экрана_20240403_223146.png)

## 7. Исполняем Brainfuck

Полученную программу запускаем в любом Brainfuck-интерпретаторе. Например, через `copy.sh/brainfuck`.

Результат выполнения:

```text
https://github.com/mihailovily/ctf_for_you/tree/main
```

![Выполнение Brainfuck](документация/скриншоты/Снимок%20экрана_20240403_223232.png)

## 8. Проверяем файл `answer.zip`

В репозитории по полученной ссылке находится файл `answer.zip`. По расширению можно предположить, что это ZIP-архив, однако проверка сигнатуры показывает обратное:

```bash
file answer.zip
```

или в hex-редакторе:

```text
52 49 46 46 .... 57 41 56 45
```

Сигнатура `RIFF` и строка `WAVE` означают, что перед нами WAV-файл, только названный как `answer.zip`.

![Файл с расширением ZIP](документация/скриншоты/Снимок%20экрана_20240403_223310.png)

Переименовываем файл для удобства:

```bash
mv answer.zip answer.wav
```

## 9. Смотрим спектрограмму

Открываем `answer.wav` в Audacity. В меню дорожки выбираем отображение **Spectrogram** вместо обычной waveform.

На спектрограмме видна строка:

```text
spoiler-CTF-is-the-best
```

Это финальная подсказка, подтверждающая, что найденный ответ является флагом задачи.

![Спектрограмма](документация/скриншоты/Снимок%20экрана_20240403_223626.png)

## Ответ

```text
zssoib{b0nc4_CTF_1s_th3_b3st}
```

## Цепочка решения

```text
QR-код
  → PNG
  → метаданные и trailer после IEND
  → Atbash
  → Base64
  → Pastebin
  → замена символов
  → Brainfuck
  → GitHub
  → WAV под расширением .zip
  → спектрограмма
  → zssoib{b0nc4_CTF_1s_th3_b3st}
```
